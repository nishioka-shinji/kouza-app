# 2026-09-15

## 講座運営アプリ 段階 5（LINE Webhook で画像を受信し R2 に保存）

設計書 7 章の段階 5 を完了した。トークに送った画像が R2 と D1 に入り、友だち追加で受講生が自動登録され、応答メッセージで LIFF へのリンクが届くところまでを実装し、実機で確認した。PR #11 としてマージ済み。

R2 を有効化して `kouza-app-images` を作り、バインディング名は `BUCKET` にした。D1 の `DB` と同じく、wrangler が提案する長い名前ではなく短い名前を採っている。

### 処理順序が段階 5 の本体

応答メッセージを署名検証の直後、200 を返す前に送る。応答トークンは 1 回限りで、時間制限に依存した実装をしてはならないと公式が明示しているため、画像の取得を挟めない。この順にすると応答時点で画像レコードがまだ無いので、リンクには画像 ID を載せられず LIFF の未メモ一覧へのリンクになる。

follow イベントも同じ扱いにした。当初の計画ではプロフィール取得と UPSERT を応答の前に置いていたが、調査で Webhook のタイムアウトが 2 秒だと分かったため、応答を先に送って残りを `waitUntil()` に逃がした。取得と UPSERT と応答を直列にすると 2 秒に迫る。

### 公式ドキュメントで確認して設計が変わった点

コンテンツ取得だけホストが `api-data.line.me` で、応答メッセージやプロフィール取得の `api.line.me` と違う。公式が明示的に注意書きを置いている箇所で、取り違えると 404 になる。設計書 8 章の表に追記した。

Webhook の再送はデフォルト無効だった。有効化はコンソールの明示操作が要る。今回はオンにしたが、再送は 2xx を返さなかった場合にしか起きないため、正常動作している限り自然には発生しない。

LINE の検証ボタンや疎通確認では `events` が空配列で飛んでくる。固定の replyToken を持つダミーイベントという記載は公式ドキュメント本文には見つからなかった。空配列を 200 で返せば足りる。

### follow に歓迎メッセージを返すことにした

設計書 F1 は follow について自動登録しか書いておらず、応答の有無に言及がなかった。無反応だと登録できたか受講生から見て分からないため、歓迎メッセージを返すことにした。応答メッセージなので課金対象外。

### 表示名は取れたときだけ上書きする

レビューで指摘を受けた。`students` の UPSERT を `ON CONFLICT DO NOTHING` で書くと、`getProfile` が失敗して `(名前未取得)` で行が確定したあと、表示名が二度と入らなくなる。開発者自身には follow が飛ばず最初の行が画像送信の経路でできるため、実際に踏みうる経路だった。

`DO UPDATE SET name = excluded.name WHERE excluded.name <> '(名前未取得)'` として、実名が取れたときだけ上書きする形にした。無駄な API 往復という当初の見立てより深い問題で、レビューを通した価値があった。実機でも `(名前未取得)` から実名への上書きが起きることを本番データで確認できている。

### 型だけ借りるために devDependencies に入れた

`@line/bot-sdk` は設計書の方針どおり `import type` で型だけ借りるが、未インストールでは型解決できないため `devDependencies` に追加した。実行時の依存は増えないので、ライブラリを増やさない方針とは衝突しない。値の import を書くと Node 依存がバンドルされて Workers で動かなくなる。

### 実機確認で 2 度つまずいた

どちらも本番に登録した値が `.dev.vars` と食い違っていたことが原因で、コードの問題ではなかった。

1 度目はチャネルシークレット。全リクエストが署名検証で 401 になり、応答が一切返らなかった。このとき手元に届いていたのは LINE 公式アカウントのあいさつメッセージで、実装した歓迎メッセージではなかった。スクリーンショットを見て文面が違うことに気づき、切り分けが進んだ。

原因特定を遅らせたのは `wrangler tail` の既定の表示で、`- Ok` は「Worker が例外なく応答した」という意味でしかなく HTTP ステータスではない。401 でも `Ok` と出る。`--format json` に切り替えるとレスポンスのステータスが見えて確定した。Webhook が無反応なときは最初からこちらで見る。

2 度目はアクセストークン。署名検証は通るので Webhook 自体は動いて見えるが、`getProfile` が失敗して表示名が `(名前未取得)` のまま入った。ローカルのトークンで直接 API を叩くと 200 が返ったことから、本番側の値が違うと判断した。

どちらも `wrangler secret put` のプロンプトへ手で貼り直すと再発しうる。検証済みの `.dev.vars` からパイプで流し込む形に変えて解決した。

### 自分の検証コマンドが誤っていた

`wrangler r2 object get` に `--remote` を付けていなかったため、実際には保存されているオブジェクトを「存在しない」と誤判定した。ローカルのエミュレート先を見ていたのが理由。CLAUDE.md の検証コマンドの表を修正した。

同じ表に `r2 object list` を書こうとして、そのサブコマンドが存在しないことも実行して分かった（`get` / `put` / `delete` のみ）。保存されたキーの一覧は D1 の `images.r2_key` で見る。

### 申し送り

応答メッセージのリンクは最終形で出しているため、段階 6 で `/liff/images` を実装するまでは 404 に着地する。段階 5 の完了条件はリンクが届くことなので条件は満たしている。

冪等性は実地で確認できていない。再送が 2xx を返さなかった場合にしか起きず、正常動作している限り発生しないため。同じ画像を手で 2 回送っても `line_message_id` が変わるので検証にならない。`images.line_message_id` の UNIQUE と決定的な `r2_key` で担保している状態にとどまる。

本番に残っているのは受講生 1 件（実 ID）、画像 1 件、講座回 1 件。画像の `lesson_id` と `memo_body` は NULL なので、段階 6 の未メモ一覧の絞り込みにそのまま使える。

段階 3 の申し送りにあったテスト行は削除済み。

ループ: reviewer 差し戻し 1 回 / integrator 省略 / verifier 対象外

## 講座運営アプリ 段階 6（LIFF でメモ入力と画像一覧、ID トークン検証）

設計書 7 章の段階 6 を完了した。受講生が LINE 内で自分の画像を見て、講座回を選んでメモを書けるところまでを実装し、実機で確認した。PR #13 としてマージしたあと、実機確認で見つかった不具合を #14〜#17 で直し、設計書の更新を #18 でまとめた。

実装は 3 タスクに分けて並列・直列で回した。ID トークンの検証（検証エンドポイントを叩く部品とミドルウェア）、R2 からの画像配信、LIFF の一覧とメモ入力。前 2 つは触るファイルが分かれるので並列にし、3 つ目は両方に依存するので直列にした。

### 素の form で POST する構成は取れなかった

設計書 F2 の記述を実装中に変えた。ID トークンは `liff.getIDToken()` でしか取れず、これはブラウザで `liff.init()` が終わった後にしか手に入らない。サーバーは最初の GET を受けた時点で送信者を特定できず、hidden input に埋めるにしても値を入れるのは JS になる。一覧の GET では hidden input 自体が置けない。

殻（検証なしで空の HTML を返す）と中身の API（検証ありで HTML 断片を返す）に分ける構成にした。`<form>` のマークアップは素のまま保ち、`submit` を乗っ取って `FormData` を送るので、クライアント側のフレームワークを使わない方針は維持できている。

### エンドポイント URL を 302 で塞いで LIFF を壊した

段階 6 でいちばん時間を取られた。実機で LIFF URL のパスが失われ、どの URL を開いても一覧に着地する事象が出た。

原因は自分が入れた修正だった。段階 4 の疎通確認ページを削除したことでエンドポイント URL（`/liff`）が 404 になり、それを 302 リダイレクトで塞いだ。LIFF はパスを `liff.state` に畳んでエンドポイント URL へ渡し、そこで実行された `liff.init()` が展開して本来の URL へ飛ばす。2 段目を行うのはブラウザ上の SDK であってサーバーではない。302 を返すと `init()` が走らず、`liff.state` が読まれないまま破棄される。公式リファレンスは `init()` の resolve 前に 301 / 302 を返すことを明示的に禁止していた。

外部ブラウザでのログインループも同じ原因だったが、最初は `liff.login()` の重複呼び出しだと誤診して削除した。それでは直らず、公式ドキュメントを調べ直して初めて根本に行き着いた。誤診した修正を 1 本無駄に挟んでいる。

404 を塞ぐ判断そのものは正しかった。手段を確かめずに選んだのが誤りで、先に公式ドキュメントを読んでいれば避けられた。段階 4 では調査と設定をコードより先に独立させた判断が効いたと書いているのに、同じ教訓を活かせていない。

### 所有者検証は SQL で絞る

画像 ID は URL 経由で書き換えられるため、`WHERE images.id = ? AND images.student_id = ?` として所有者が一致する行だけを返す。不一致と不存在をまとめて 401 に潰し、画像の存在の有無を漏らさない。判定に使うのは検証済みの LINE ユーザー ID のみで、クライアント由来の値は一切使わない。

reviewer が実データで裏を取っている。本人が他人の画像を要求すると 0 行になり、同じ行を真の所有者が引くと 1 行返る。存在するのに他人には隠せていることまで確認できた。

### JSON.stringify は `</script>` をエスケープしない

implementer が指示にない箇所を 1 つ直していた。画像 ID をインラインスクリプトに埋める以上、`</script><img src=x>` のような ID で script 要素から脱出できる。`<` を `<` に潰して塞いでいる。指示に書いていなかった穴を実装者が見つけて対処した形で、そのまま採用した。

### 実機でしか見つからなかったもの

絞り込みの導線が片道だった。未メモのみ表示から全件へは戻れるが、逆がない。応答メッセージのリンクを踏み直すかブラウザバックするしかなかった。状態表示を対称にして往復できるようにした。

`<a href>` での遷移はページを読み込み直すため、そのたびに `liff.init()` が走る。fetch で中身だけ差し替えれば初期化は初回のみで済むが、快適さの改善であり完了条件に関わらないので見送った。段階 7 以降で扱う。

### 401 の確認には本番にダミーを入れた

本番には受講生も画像も自分の 1 件しかなく、他人の画像が存在しない。完了条件の「他人の画像 ID を指定すると 401」を実地で確かめるため、ダミーの受講生と画像を 1 件ずつ入れて確認し、直後に削除した。段階 3 でテスト行を入れて後で消したのと同じやり方。

同じ形式の URL で、他人の画像は弾かれ自分の画像は開けた。URL の違いは UUID だけなので、所有者検証が効いていることの証拠になる。

### 申し送り

画像配信（`/images/{uuid}`）は認証なしで、UUID の推測困難性に頼っている。設計書で合意済みの割り切りだが、段階 6 で初めて画像が外部から取得可能になった。

`memo_body` に長さの上限がない。D1 の行サイズ上限に当たれば 500 になる。個人開発の想定では問題にならないが、サーバー側で上限を決めておくと将来の事故を防げる。

`received_at` などの日時は UTC のまま表示している。管理画面も同じなので一貫はしているが、日本時間との 9 時間差が見える。表示形式の統一は別途まとめて扱う。

worktree には `.dev.vars` が引き継がれない。gitignore されているため当然だが、`wrangler types` が secret の型をローカルの `.dev.vars` から拾うので、無いと `Env` から secret が欠落して webhook 側で型エラーが出る。実装タスク 2 つが同じ罠を踏んだ。worktree を作ったら `.dev.vars` をコピーしてから `cf-typegen` を実行する必要がある。

ループ: reviewer 差し戻し 0 回 / integrator 省略 / verifier 対象外

### ループの振り返り
- 事象: reviewer は 3 タスクとも PASS（指摘は minor 1 件のみ）。PR #13 をマージして実機確認に入ってから、LIFF のパス解決の破綻・外部ブラウザのログインループ・絞り込みの片道導線の 3 件が出て、追加 PR を 4 本要した。うち #14 と #16 は誤診による修正で、#17 で初めて根本原因に到達した
- 分類: 知識不足。LIFF の URL 解決が 2 段階で、2 段目を `liff.init()` が行うという仕様を知らないまま `/liff` の 404 を 302 で塞いだ。公式リファレンスには `init()` の resolve 前のリダイレクト禁止が明記されていたが、実装前にも修正前にも読んでいない。reviewer と integrator は Git 上の静的検査なので、この種の外部サービスの仕様違反は原理的に捕まえられない
- 反映: 今回は記録のみ。知識不足の分類は初回。次に同じ分類が出たら、リポジトリの CLAUDE.md に「外部サービスの挙動に依存する修正は、実装前に公式ドキュメントで裏を取る」を追加することを提案する。なお段階 4 で「調査をコードより先に独立させた判断が効いた」と書いた教訓が、段階 6 で活かされなかった点は記録しておく

## 講座運営アプリ 段階 7（管理画面で画像・メモを通覧）

設計書 7 章の段階 7 を完了した。運営者が受講生ごと・講座回ごとに画像とメモを見られるところまでを実装し、デプロイして実機で確認した。PR #20 としてマージし、設計書の更新を #21 で出している。

あわせて段階 6 の申し送り 2 件を回収した。日時が UTC のままだった件と、`memo_body` に長さ上限がなかった件。

実装は 3 タスクに分けたが、worktree は 1 本で直列に回した。T1 が T2・T3 の両方とファイルが重なるうえ、3 つとも小さく、並列化の利得より worktree 管理のコストが勝ると判断したため。分割の目的は並列化ではなくレビュー単位の明確化に置いた。

### 一覧は 1 枚にして受講生詳細ページを作らない

完了条件の「受講生ごと・講座回ごと」は、`/admin/images` にセレクトを 2 つ置けば 1 画面で満たせる。受講生詳細ページを作ると導線は厚くなるが、講座回ごとの横断表示には別途画面が要る。1 枚に寄せた。

カードにはメモ本文を出すことにした。有無だけの表示にすると 1 件ずつ開く画面が欲しくなり、作らないと決めた受講生詳細ページを呼び込んでしまうため。

### 管理画面の画像は Access 配下の別経路から配信する

`<img>` が参照するのは `/admin/images/{uuid}`。配信の本体を `serveImage` として切り出し、無認証の `/images/{uuid}` と共有した。受講生向けの経路は F3 で合意済みの割り切りのまま変更していない。

Access の宛先追加は不要だった。既存の `/admin/*` が新設の 2 パスを覆う。ただしこれはドキュメントからの推論だったので、デプロイ後に実機で 302 を確認するまで保護済みとは言わないことにした。一覧ページだけでなく画像 URL 側も確認している。ここが覆っていなければ画像が無認証で露出するため。

段階 6 で「手段を確かめずに選んだ」失敗をしているので、今回は推論と実測を分けて扱った。

### 日時は表示だけを JST にする

DB の値も SQL の並び順も UTC のまま触らない。`lessons.held_on` は日付文字列なので変換してはいけない。通すと開催日が全件 1 日前にずれる。implementer が「日時らしいものを全部変換する」動きをしないよう、指示と制約に明記した。

LIFF 側は `received_at` を取得しながら画面に出していなかったことが分かり、変換ではなく表示の追加になった。planner が気づいた点で、私はコードを読んでいながら見落としていた。

### レビューで直した 2 件

どちらも実装者の検証が経路を外していたために残ったもの。

1 件目は絞り込みフォームで「すべて」を選ぶと一覧が空になる不具合。`Number("")` が `0` を返し `Number.isInteger(0)` が真になるため、空の `<option value="">` が id 0 での絞り込みに化けていた。画面の絞り込み UI を使えば必ず踏む経路だったが、実装者の検証が URL 直打ちのみでフォームが実際に送る形を通していなかった。

2 件目は 400 で弾いたときに入力が完全に失われる問題。`MemoForm` に渡す `image` が DB から読んだ行のままで、2000 文字超を書いた利用者が本文を全部失う。既存の講座回エラーの分岐も同じ作りだったので揃えて直した。私が T3 の指示に「既存の流儀に揃える」と書いたことが、結果的に既存の欠陥を複製させた形になっている。

### 差し戻しの根拠を一部誤っていた

2 件目を差し戻したとき、私は「絵文字がブラウザの `maxlength` を通過してサーバーで弾かれるので日常的に踏む」と説明した。reviewer が再レビューで自分の前回指摘ごと訂正し、`maxlength` も `String.prototype.length` も UTF-16 基準で両者にずれはない、が正しい結論になった。

差し戻し自体は妥当で、入力消失は実在する。ただし到達経路は私の説明より稀で、フォームを介さない POST に限られる。誤った根拠で重大度を語った点は記録しておく。

### メモ上限は実機で動かせていない

サーバー側の長さ検証を本番の HTTP 経路で発火させられなかった。`maxlength` がブラウザ側で貼り付けを切り詰めるため通常操作では到達せず、到達させるには有効な ID トークンを付けた POST が要る。ID トークンは LINE アプリ内でしか発行できず、こちらのツールからは作れない。

実装はレビューで確認済みで、400 の応答で送信値が差し戻されることもコンポーネントのレンダリングで検証したが、実経路を通した確認ではない。設計書にも確認できていないこととして残した。

### 絞り込みの確認にはダミーを入れた

本番には受講生も講座回も 1 件ずつしかなく、そのままでは絞り込めている証拠にならない。ダミーの受講生・講座回・画像を 1 件ずつ入れて両軸を確認し、直後に削除した。段階 3・6 と同じやり方。削除後に件数が投入前へ戻ることと、行レベルで残骸がないことまで確認している。

管理画面は Access に守られているため、こちらの `curl` からは 302 で中身を見られない。絞り込みが効いているかの確認は認証済みブラウザを持つ側にしか行えず、そこは依頼した。

### 申し送り

`ORDER BY images.received_at` にインデックスがなく、絞り込みなしの一覧は全件スキャンと TEMP B-TREE ソートになる。D1 はスキャンした行数で課金されるため、画像が数千件に育つと一覧のたびに効いてくる。5 章のインデックス表を変える話なので、ページネーションを入れる段階で表ごと見直す。

メモの文字数は UTF-16 コード単位で数えている。サロゲートペア（一部の漢字・絵文字）は 1 文字が 2 と数えられ、体感の半分で上限に当たる。実害は 2000 字まで書けないことだけなので現状維持とした。

ループ: reviewer 差し戻し 2 回 / integrator PASS / verifier 対象外

### ループの振り返り
- 事象: T2 と T3 で 1 回ずつ差し戻した。T2 は空文字が id 0 での絞り込みに化けてフォーム経由の一覧が常に空になる不具合、T3 は 400 応答でユーザーの入力が消える問題。いずれも reviewer 到達前に本体が実サーバーで再現を確認して差し戻した。integrator は PASS で指摘ゼロ
- 分類: 指示不足。T2 は implementer への指示に「フォームが実際に送る形（空文字を含む）で検証する」と書いておらず、URL 直打ちの期待件数表だけを渡していた。T3 は「既存の流儀に揃える」と指示したことで、既存の講座回エラー分岐が持っていた入力消失の欠陥をそのまま複製させた
- 反映: 空文字の扱いと検証方法をリポジトリの CLAUDE.md の実装上の制約に追加した（PR #21）。指示不足の分類は段階 3 に続いて 2 回目のため。段階 3 は空文字の氏名が保存される件で、今回は空文字が `Number()` で `0` になる件。同じ「空文字を詰めていない」系統が別の形で再発している
- 補足: 「既存の流儀に揃える」という指示は、既存側に欠陥がある場合それを複製させる。既存コードを手本として指す際は、その箇所が正しいことを確認してから指す必要がある
